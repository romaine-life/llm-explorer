apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-explorer
  namespace: llm-explorer
  labels:
    app: llm-explorer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-explorer
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: llm-explorer
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: infra-shared
      containers:
        - name: llm-explorer
          image: romainecr.azurecr.io/llm-explorer:placeholder
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: AZURE_APP_CONFIG_ENDPOINT
              value: https://infra-appconfig.azconfig.io
            - name: KEY_VAULT_URL
              value: https://ng6-llm-explorer.vault.azure.net/
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            failureThreshold: 20
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            failureThreshold: 3
